name: Reusable deployment

on: workflow_call

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |
          echo "Output github object..."
         echo "${{toJson(github)}}"
